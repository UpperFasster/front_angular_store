export interface IProduct {
  id: string;
  name: string;
  price: number;
  discount: number;
  currency: string;
  photo: string;
  additionalImages: Array<string>;
  quantity: number;
  description: number;
}